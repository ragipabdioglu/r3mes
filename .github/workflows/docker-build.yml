name: Docker Build

# Devre dışı - sadece manuel tetikleme
on:
  workflow_dispatch:

jobs:
  build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Info
        run: echo "Docker build workflow - manual trigger only"
