name: Lint

# Devre dışı - sadece manuel tetikleme
on:
  workflow_dispatch:

jobs:
  lint:
    name: Lint Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Info
        run: echo "Lint workflow - manual trigger only"
