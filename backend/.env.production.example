# R3MES Backend Production Environment Configuration
# 
# IMPORTANT: This is an EXAMPLE file. Copy to .env.production and configure with your actual values.
# NEVER commit .env.production to version control (it's in .gitignore).
#
# Production Security Requirements:
# - R3MES_USE_MOCK_MODEL must be false (enforced by code)
# - FAUCET_ENABLED should be false unless specifically needed
# - All URLs must point to production services (not localhost)
# - All secrets must be provided via secure environment variables

# Environment
R3MES_ENV=production

# Database Configuration
DATABASE_TYPE=postgresql
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
# SECURITY: POSTGRES_PASSWORD must be set via environment variable, not hardcoded here

# Redis Configuration
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
# SECURITY: REDIS_PASSWORD must be set via environment variable, not hardcoded here

# Blockchain Configuration
# IMPORTANT: Replace with your actual blockchain RPC endpoint
BLOCKCHAIN_RPC_URL=https://rpc.r3mes.network:26657
BLOCKCHAIN_REST_URL=https://rest.r3mes.network:1317
CHAIN_ID=remes-mainnet-1

# CORS Configuration
# IMPORTANT: Replace with your actual production frontend URL
CORS_ORIGINS=https://r3mes.network,https://www.r3mes.network

# Model Configuration
# SECURITY: Mock model is FORBIDDEN in production (code will fail fast if enabled)
R3MES_USE_MOCK_MODEL=false
# Provide one of: R3MES_MODEL_IPFS_HASH, R3MES_MODEL_NAME, or BASE_MODEL_PATH
# R3MES_MODEL_IPFS_HASH=Qm...
# R3MES_MODEL_NAME=bitnet-1b
# BASE_MODEL_PATH=/path/to/model

# Faucet Configuration
# IMPORTANT: Disable faucet in production unless specifically needed
FAUCET_ENABLED=false
FAUCET_AMOUNT=1000000uremes
FAUCET_TREASURY_ADDRESS=
FAUCET_KEY_NAME=faucet-key
FAUCET_KEYRING_BACKEND=os
# IMPORTANT: REMESD_PATH and REMESD_HOME should be configured per deployment
# Do not hardcode paths - use environment variables
REMESD_PATH=remesd
REMESD_HOME=/opt/remesd

# Logging
LOG_LEVEL=INFO

# Monitoring & Observability
SENTRY_DSN=
PROMETHEUS_ENABLED=true

# Security
ALLOWED_HOSTS=r3mes.network,www.r3mes.network
