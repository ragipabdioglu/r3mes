# R3MES Docker Compose Environment Configuration
# 
# Copy this file to .env in the docker/ directory and fill in actual values.
# NEVER commit .env to version control (it's in .gitignore).
#
# SECURITY: All passwords and secrets must be provided via environment variables.
# Do not hardcode sensitive values in this file.

# ============================================================================
# PostgreSQL Configuration
# ============================================================================
POSTGRES_USER=r3mes
# SECURITY: POSTGRES_PASSWORD must be set (use strong password in production)
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=r3mes
POSTGRES_PORT=5432

# ============================================================================
# Redis Configuration
# ============================================================================
# SECURITY: REDIS_PASSWORD must be set (use strong password in production)
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_PORT=6379

# ============================================================================
# Backend Configuration
# ============================================================================
BACKEND_PORT=8000
R3MES_ENV=development

# Blockchain Configuration
# IMPORTANT: Replace with your actual blockchain RPC endpoint
# For Docker Compose, use service name if blockchain is in same compose file
# Example: http://remesd:26657 (if remesd service exists)
BLOCKCHAIN_RPC_URL=http://localhost:26657

# ============================================================================
# Frontend Configuration
# ============================================================================
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Analytics and Monitoring
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_SENTRY_DSN=
