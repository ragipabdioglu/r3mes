# R3MES Blockchain Node Development Environment Configuration
# Copy this file to .env and fill in actual values for local development

# ============================================================================
# Environment Mode
# ============================================================================
R3MES_ENV=development

# ============================================================================
# gRPC Configuration
# ============================================================================
# gRPC server address
# In development, localhost is acceptable
R3MES_GRPC_ADDR=localhost:9090

# ============================================================================
# Tendermint RPC Configuration
# ============================================================================
# Tendermint RPC address
# In development, localhost is acceptable
R3MES_TENDERMINT_RPC_ADDR=http://localhost:26657

# ============================================================================
# CORS Configuration
# ============================================================================
# Comma-separated list of allowed CORS origins
# In development, localhost is acceptable
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# ============================================================================
# Miner Stats Service Configuration
# ============================================================================
# Host for miner stats service
# In development, localhost is acceptable
MINER_STATS_HOST=localhost

# Port for miner stats service
MINER_STATS_PORT=8080

# ============================================================================
# Verification Service Configuration
# ============================================================================
# Verification service address
# In development, localhost is acceptable
R3MES_VERIFICATION_SERVICE_ADDR=localhost:50051

# ============================================================================
# Important Notes
# ============================================================================
# 1. This is a development configuration file
# 2. For production, use .env.production.example as a template
# 3. Never commit actual .env files to Git
# 4. In production, all URLs must use actual hostnames (no localhost)
# 5. In production, CORS_ALLOWED_ORIGINS cannot use wildcard '*'
# 6. In production, all addresses must be accessible from external networks
