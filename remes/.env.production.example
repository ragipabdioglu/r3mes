# R3MES Blockchain Node Production Environment Configuration
# Copy this file to .env and fill in actual values for production deployment

# ============================================================================
# Environment Mode
# ============================================================================
R3MES_ENV=production

# ============================================================================
# gRPC Configuration
# ============================================================================
# gRPC server address
# REQUIRED in production - do NOT use localhost
R3MES_GRPC_ADDR=blockchain.example.com:9090

# ============================================================================
# Tendermint RPC Configuration
# ============================================================================
# Tendermint RPC address
# REQUIRED in production - do NOT use localhost
R3MES_TENDERMINT_RPC_ADDR=https://rpc.example.com:26657

# ============================================================================
# CORS Configuration
# ============================================================================
# Comma-separated list of allowed CORS origins
# REQUIRED in production - do NOT use localhost or wildcard '*'
CORS_ALLOWED_ORIGINS=https://app.r3mes.network,https://www.r3mes.network

# ============================================================================
# Miner Stats Service Configuration
# ============================================================================
# Host for miner stats service
# REQUIRED in production - do NOT use localhost
MINER_STATS_HOST=miner-stats.example.com

# Port for miner stats service
MINER_STATS_PORT=8080

# ============================================================================
# Verification Service Configuration
# ============================================================================
# Verification service address
# REQUIRED in production - do NOT use localhost
R3MES_VERIFICATION_SERVICE_ADDR=verification.example.com:50051

# ============================================================================
# Important Notes
# ============================================================================
# 1. This is a production configuration file
# 2. All URLs must use actual hostnames (no localhost or 127.0.0.1)
# 3. CORS_ALLOWED_ORIGINS cannot use wildcard '*'
# 4. All addresses must be accessible from external networks
# 5. Use HTTPS/WSS for all external URLs in production
# 6. Store sensitive values in a secret management service (Vault, AWS Secrets Manager, etc.)
