# R3MES Web Dashboard Development Environment Configuration
# Copy this file to .env.local and fill in actual values for local development

# ============================================================================
# Environment Mode
# ============================================================================
NODE_ENV=development
R3MES_ENV=development

# ============================================================================
# Backend API Configuration
# ============================================================================
# Backend API URL (for chat, user info, etc.)
# In development, localhost is acceptable
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Backend API URL for server-side proxy (internal network)
# This is used by the Next.js API routes to proxy requests to the backend
BACKEND_API_URL=http://localhost:8000

# ============================================================================
# Blockchain API Configuration
# ============================================================================
# Blockchain REST API URL (Cosmos SDK REST API)
# In development, localhost is acceptable
NEXT_PUBLIC_API_URL=http://localhost:1317

# Alternative blockchain REST API URL
NEXT_PUBLIC_BLOCKCHAIN_REST_URL=http://localhost:1317

# Blockchain RPC URL (Tendermint RPC)
# In development, localhost is acceptable
NEXT_PUBLIC_BLOCKCHAIN_RPC_URL=http://localhost:26657

# WebSocket URL (for real-time updates)
# In development, localhost is acceptable
NEXT_PUBLIC_WS_URL=ws://localhost:1317

# ============================================================================
# Site Configuration
# ============================================================================
# Site URL (for canonical URLs, Open Graph, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics Tracking ID (optional)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Verification (for Search Console, optional)
# NEXT_PUBLIC_GOOGLE_VERIFICATION=

# ============================================================================
# Rate Limiting
# ============================================================================
# Rate limit window in milliseconds
RATE_LIMIT_WINDOW_MS=60000

# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# Sentry Error Tracking (Optional)
# ============================================================================
# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# NEXT_PUBLIC_SENTRY_DEBUG=false

# ============================================================================
# Build Configuration
# ============================================================================
# Build ID (optional, auto-generated if not set)
# BUILD_ID=build-$(date +%s)

# ============================================================================
# Playwright Testing
# ============================================================================
# Base URL for Playwright tests
# PLAYWRIGHT_BASE_URL=http://localhost:3000

# ============================================================================
# Important Notes
# ============================================================================
# 1. This is a development configuration file
# 2. For production, use env.production.example as a template
# 3. Never commit actual .env.local files to Git
# 4. All NEXT_PUBLIC_* variables are exposed to the browser
# 5. In production, localhost URLs are NOT allowed
# 6. WebSocket URLs should use wss:// (secure) in production
# 7. Backend and blockchain URLs should use https:// in production
