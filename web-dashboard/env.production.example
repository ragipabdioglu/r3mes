# R3MES Web Dashboard Production Environment Configuration
# Copy this file to .env.local or .env.production and fill in actual values

# ============================================================================
# Environment Mode
# ============================================================================
NODE_ENV=production
R3MES_ENV=production

# ============================================================================
# Backend API Configuration
# ============================================================================
# Backend API URL (for chat, user info, etc.)
NEXT_PUBLIC_BACKEND_URL=https://api.r3mes.network

# Production backend URL (server-side, for rewrites)
PRODUCTION_BACKEND_URL=https://api.r3mes.network

# ============================================================================
# Blockchain API Configuration
# ============================================================================
# Blockchain REST API URL (Cosmos SDK REST API)
NEXT_PUBLIC_API_URL=https://rpc.r3mes.network
PRODUCTION_BLOCKCHAIN_RPC_URL=https://rpc.r3mes.network

# Blockchain RPC URL (Tendermint RPC)
NEXT_PUBLIC_BLOCKCHAIN_RPC_URL=https://rpc.r3mes.network:26657

# Blockchain REST API URL (alternative)
NEXT_PUBLIC_BLOCKCHAIN_REST_URL=https://rpc.r3mes.network

# WebSocket URL (for real-time updates)
NEXT_PUBLIC_WS_URL=wss://rpc.r3mes.network

# ============================================================================
# Site Configuration
# ============================================================================
NEXT_PUBLIC_SITE_URL=https://r3mes.network

# Google Analytics (optional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Verification (for Search Console)
NEXT_PUBLIC_GOOGLE_VERIFICATION=

# ============================================================================
# Rate Limiting
# ============================================================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# Sentry Error Tracking (Optional)
# ============================================================================
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DEBUG=false

# ============================================================================
# Build Configuration
# ============================================================================
BUILD_ID=build-$(date +%s)

# ============================================================================
# Important Notes
# ============================================================================
# 1. All NEXT_PUBLIC_* variables are required in production
# 2. Do NOT use localhost URLs in production - they will cause failures
# 3. WebSocket URLs should use wss:// (secure) in production
# 4. Backend and blockchain URLs should use https:// in production
# 5. Copy this file to .env.local or .env.production and fill in actual values

